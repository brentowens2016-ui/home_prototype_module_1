# Example environment file for secure storage
#
# Generate a 32-byte key and base64 encode it (e.g., using Python):
#   import base64, os; print(base64.urlsafe_b64encode(os.urandom(32)).decode())
#
# Copy the output below:
SECURE_STORAGE_KEY=your_base64_32byte_key_here
